// add functon
const add = (first, second) => {
  return first + second;
};
const malti = (first, second) => {
  return first * second;
};
const sub = (first, second) => {
  return first - second;
};
const div = (first, second) => {
  return first / second;
};

export { add, div, malti, sub };
